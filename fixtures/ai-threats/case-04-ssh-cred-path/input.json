{
  "mcpServers": {
    "fs-aws": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem@1.0.3", "~/.aws/credentials"],
      "description": "Filesystem access scoped to a credentials file"
    }
  }
}
